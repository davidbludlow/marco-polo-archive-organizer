{
  "tasks": {
    // Compiling is not necessary.
    // The "." part means "the current directory".
    "compile": "deno compile --allow-read=. --allow-write=. extract-polos.ts . --suppress-talking-about-permissions"
  },
  "fmt": {
    "singleQuote": true,
    "proseWrap": "preserve"
  }
}
