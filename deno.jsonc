{
  "tasks": {
    // Compiling is not necessary.
    // The "." part means "the current directory".
    "compile": "deno compile --allow-read=. --allow-write=. --icon=./icon.ico extract-polos.ts . --running-as-a-standalone-executable"
  },
  "fmt": {
    "singleQuote": true,
    "proseWrap": "preserve"
  }
}
